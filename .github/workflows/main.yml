name: Fortify FoD DAST Automated Scan
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  security-events: write   # Required to publish to GitHub Security tab

env:
  FOD_URL: ${{ vars.FOD_URL }}
  FOD_CLIENT_ID: ${{ secrets.FOD_CLIENT_ID }}
  FOD_CLIENT_SECRET: ${{ secrets.FOD_CLIENT_SECRET }}
  FOD_RELEASE: ${{ vars.FOD_RELEASE }}

jobs:
  fod-dast-scan:
    runs-on: ubuntu-latest

    steps:
      # Install fcli via the official Fortify setup action
      - name: Setup Fortify tools
        uses: fortify/github-action/setup@v1
        with:
          fcli: latest

      # Sanitize FOD_URL: fall back to US data center if variable is empty,
      # and ensure the URL always starts with https://.
      - name: Sanitize FOD_URL
        run: |
          RAW_URL="${FOD_URL}"
          if [[ -z "$RAW_URL" ]]; then
            RAW_URL="https://api.ams.fortify.com"
          fi
          RAW_URL="${RAW_URL%/}"
          if [[ "$RAW_URL" != https://* && "$RAW_URL" != http://* ]]; then
            RAW_URL="https://${RAW_URL}"
          fi
          echo "Resolved FOD_URL: ${RAW_URL}"
          echo "FOD_URL=${RAW_URL}" >> $GITHUB_ENV

      # Authenticate to Fortify on Demand
      - name: Login to Fortify on Demand
        run: |
          fcli fod session login \
            --url "$FOD_URL" \
            --client-id "$FOD_CLIENT_ID" \
            --client-secret "$FOD_CLIENT_SECRET"

      # Validate FOD_RELEASE is set
      - name: Validate FOD_RELEASE
        run: |
          if [[ -z "$FOD_RELEASE" ]]; then
            echo "ERROR: FOD_RELEASE is not set."
            echo "Go to: GitHub repo → Settings → Variables → Actions → New repository variable"
            echo "  Name:  FOD_RELEASE"
            echo "  Value: <AppName>:<ReleaseName>  e.g.  ZeroBank:main"
            echo ""
            echo "Available releases in your FoD tenant:"
            fcli fod release list -o table 2>/dev/null || true
            exit 1
          fi
          echo "FOD_RELEASE: $FOD_RELEASE"

      # Diagnose: print all available assessment types using the FoD REST lookup
      # This helps identify the correct assessment type name for your tenant.
      - name: List Available Assessment Types (Diagnostic)
        run: |
          echo "=== All available assessment types in this FoD tenant ==="
          fcli fod rest lookup DastAutomatedAssessmentTypes -o table 2>/dev/null || \
          fcli fod rest lookup AssessmentTypes -o table 2>/dev/null || \
          echo "Could not retrieve assessment types via lookup; will use release lsat output below."

          echo ""
          echo "=== Assessment types for release: $FOD_RELEASE ==="
          fcli fod release list-assessment-types \
            --rel "$FOD_RELEASE" \
            -o table 2>/dev/null || echo "No assessment types returned for this release."

          echo ""
          echo "=== All scan types for release: $FOD_RELEASE ==="
          fcli fod release list-assessment-types \
            --rel "$FOD_RELEASE" \
            --scan-types Static,Dynamic,Mobile,OpenSource,Container \
            -o table 2>/dev/null || echo "No scan types returned."

      # Configure the DAST Automated Website scan settings.
      # --assessment-type accepts either the Name or the Id of the assessment type.
      # "DAST Automated" is the standard name in most FoD tenants.
      # If this step fails, check the "List Available Assessment Types" step above
      # to find the exact name for your tenant and set it as vars.FOD_ASSESSMENT_TYPE.
      - name: Configure DAST Automated Scan Settings
        run: |
          # Use vars.FOD_ASSESSMENT_TYPE if set, otherwise try the standard name
          ASSESSMENT_TYPE="${{ vars.FOD_ASSESSMENT_TYPE }}"
          if [[ -z "$ASSESSMENT_TYPE" ]]; then
            ASSESSMENT_TYPE="DAST Automated"
          fi

          echo "Setting up DAST Website scan:"
          echo "  Release:         $FOD_RELEASE"
          echo "  Assessment Type: $ASSESSMENT_TYPE"
          echo "  Target URL:      http://zero.webappsecurity.com/"

          fcli fod dast-scan setup-website \
            --rel "$FOD_RELEASE" \
            --url "http://zero.webappsecurity.com/" \
            --assessment-type "$ASSESSMENT_TYPE" \
            --frequency Subscription \
            --skip-if-exists

      # Start the DAST Automated scan
      - name: Start FoD DAST Automated Scan
        run: |
          fcli fod dast-scan start \
            --rel "$FOD_RELEASE" \
            --store curScan

      # Poll until scan completes
      - name: Wait for DAST Scan Completion
        run: |
          fcli fod dast-scan wait-for \
            --rel "$FOD_RELEASE"

      # Export SARIF results to GitHub Security tab
      - name: Export Results to GitHub Security
        uses: fortify/github-action/fod-export@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Always clean up the session
      - name: Logout from Fortify on Demand
        if: always()
        run: fcli fod session logout
